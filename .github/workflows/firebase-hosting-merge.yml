# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
# 
# TEMPORARILY DISABLED - Firebase hosting is suspended
# Use the production-deploy.yml workflow when Firebase is reactivated

name: "[DISABLED] Deploy to Firebase Hosting on merge"

# TEMPORARILY DISABLED - Firebase hosting is suspended
# Uncomment the 'on' section when Firebase is reactivated
# on:
#   push:
#     branches:
#       - master

on: workflow_dispatch  # Manual trigger only while disabled
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: ℹ️ Firebase hosting temporarily disabled
        run: |
          echo "Firebase hosting is temporarily suspended."
          echo "Use the 'Deploy to Production (Firebase)' workflow when it's reactivated."
          echo "Current staging environment: GitHub Pages"

  # build_and_deploy:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: npm ci && npm run build
  #     - uses: FirebaseExtended/action-hosting-deploy@v0
  #       with:
  #         repoToken: ${{ secrets.GITHUB_TOKEN }}
  #         firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TESLACENTER }}
  #         channelId: live
  #         projectId: teslacenter
