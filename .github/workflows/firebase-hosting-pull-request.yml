# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
#
# TEMPORARILY DISABLED - Firebase hosting is suspended

name: "[DISABLED] Deploy to Firebase Hosting on PR"

# TEMPORARILY DISABLED - Firebase hosting is suspended  
# Uncomment when Firebase is reactivated
# on: pull_request

on: workflow_dispatch  # Manual trigger only while disabled

permissions:
  checks: write
  contents: read
  pull-requests: write
  
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: ℹ️ Firebase PR previews temporarily disabled
        run: |
          echo "Firebase hosting is temporarily suspended."
          echo "PR previews will be available when Firebase is reactivated."
          echo "Current staging: GitHub Pages deployment on merge to master"

  # build_and_preview:
  #   if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: npm ci && npm run build
  #     - uses: FirebaseExtended/action-hosting-deploy@v0
  #       with:
  #         repoToken: ${{ secrets.GITHUB_TOKEN }}
  #         firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TESLACENTER }}
  #         projectId: teslacenter
